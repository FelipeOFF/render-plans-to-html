{
  "name": "render-plans-to-html",
  "version": "0.1.0",
  "description": "Tool-agnostic renderer that turns Markdown plans/specs/reviews into a single interactive HTML dashboard",
  "type": "module",
  "bin": {
    "render-plans": "./bin/render-plans.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "gray-matter": "^4.0.3",
    "marked": "^14.1.3"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
